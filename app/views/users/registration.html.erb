<div class="app-form">
  <% form_for @user do |f| %>
    <%= render :partial => "form", :object => f, :locals => {:register => true} %>
    <%= f.submit 'Register' %>
  <% end %>
  <%= link_to 'Back', root_url %>
</div>
