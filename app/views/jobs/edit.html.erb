<% form_for [current_user, @job] do |f| %>
  <%= f.error_messages %>
  <%= render :partial => 'form', :object => f %>
  <%= f.submit 'Update' %> | <%= link_to 'Cancel', user_jobs_path(current_user) %>
<% end %>
