<h1>New job</h1>
<%= render :partial => 'shared/notice' %>
<div class="app-form">
  <% form_for [current_user, @job] do |f| %>
    <%= render  :partial => 'form', :object => f %>
    <%= f.submit 'Add' %> | <%= link_to 'Cancel', user_jobs_path(current_user) %>
  <% end %>
</div>
